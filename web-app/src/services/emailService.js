/**
 * Email Service - Main email service for the application
 * Re-exports from lib/emailService for consistency
 */

export { emailService, clientEmailService } from '@/lib/emailService';
export { default } from '@/lib/emailService';
