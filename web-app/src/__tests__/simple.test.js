// Simple test to check if import.meta transformation is working
describe('Import Meta Test', () => {
  test('should handle import.meta transformation', () => {
    // This should work if our Babel plugin is working correctly
    expect(true).toBe(true);
  });
});
