# =============================================================================
# NEXA MANAGER - Environment Variables Configuration
# =============================================================================
# 
# IMPORTANT SECURITY NOTICE:
# 1. Copy this file to .env.local in the same directory
# 2. Fill in your actual values in .env.local
# 3. NEVER commit .env.local to version control
# 4. .env.local is already in .gitignore to prevent accidental commits
#
# =============================================================================

# Supabase Configuration (REQUIRED)
# Get these values from your Supabase project dashboard
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-from-supabase-dashboard

# Development settings
VITE_APP_ENV=development
VITE_DEBUG_MODE=true

# Optional: Custom Supabase settings
VITE_SUPABASE_REALTIME_ENABLED=true
VITE_SUPABASE_AUTH_PERSIST_SESSION=true

# =============================================================================
# SETUP INSTRUCTIONS:
# =============================================================================
# 1. cp env.example .env.local
# 2. Edit .env.local with your actual Supabase credentials
# 3. Restart your development server (npm run dev)
# 
# For production deployment:
# - Set these variables in your hosting platform environment
# - Never use hardcoded values in source code
# ============================================================================= 